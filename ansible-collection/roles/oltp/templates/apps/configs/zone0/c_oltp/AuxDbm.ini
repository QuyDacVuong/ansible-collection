; {{ ansible_managed }}
;@d:\apps\configs\c_oltp\sharedOLAPs.ini

;DATABASE CONNECTION HANDLED VIA ESL

[DYNAMICCONNECT]
 
  
[STATICCONNECT]
 
  
[MAINSTORAGE] 
dsname=ROOTAX_RW
login=db2_cdl
password={{ lookup('amazon.aws.ssm_parameter', '/{{ env }}/common/db2cdl') }}
schema=db2admin

stmtFailureLogging=1

[STATICSETTINGS] 
 
 
[DYNAMICSETTINGS] 
ClosedSessionTTLInSecs=600 
;ScavengerIntervalInSecs=15
;olapIniFileName=d:/apps/configs/c_oltp/sharedOLAPs.ini

ScavengerIntervalInSecs=0

[STARTUP]


[TABLES]
DBMID=1521

[;SETTINGS]
olap=OS127
olap=OS128
olap=OS124

[TOB]
schema=outbox
; same prefix as tobrelay.ini
prefix=main

[LOGGER]
server=ip:{{ hercules_host }}:9322
LogStationName={{ stationname }}.C_OLTP.AuxMainDbm-{{ hostvars[inventory_hostname]['private_ip_address'].split('.')[-1] }}
UseLogger=1
