[comm]
{% if oltp_comm %}
{{ oltp_comm }}
{% endif %}

[tcp]
{% if oltp_tcp %}
{{ oltp_tcp }}
{% endif %}

[tcpmux]
{% if oltp_tcpmux %}
{{ oltp_tcpmux }}
{% endif %}

[smem]
{% if oltp_smem %}
{{ oltp_smem }}
{% endif %}

[smemmux]
{% if oltp_smemmux %}
{{ oltp_smemmux }}
{% endif %}

{% if oltp_restrictconnections %}
[restrictconnections]
{{ oltp_restrictconnections }}
{% endif %}

[exe]
{% if oltp_exe %}
{{ oltp_exe }}
{% endif %}

[start]
{% if oltp_start %}
{{ oltp_start }}
{% endif %}


[logger]
{% if oltp_logger %}
{{ oltp_logger }}
{% endif %}

[telemetry.targets]
hercules = true
gateway = true
[telemetry.gateway.conn-params]
address = ip:127.0.0.1:5447
instance = TelgatewayInstance
[telemetry.metrics.tags]
application = OLTP_DISP
environment = {{ hostvars[inventory_hostname].tags.Environment }}
name = {{ hostvars[inventory_hostname].tags.Name }}
instanceid = {{ hostvars[inventory_hostname].instance_id }}
platform = {{ hostvars[inventory_hostname].tags.Platform }}
zone = {{ hostvars[inventory_hostname].tags.Zone }}
station_name = {{ stationname }}.C_OLTP.OLTP_DISP-{{ hostvars[inventory_hostname]['private_ip_address'].split('.')[-1] }}
service_name = OLTP_DISP
[telemetry.metrics.filters.1]
regex = .*
