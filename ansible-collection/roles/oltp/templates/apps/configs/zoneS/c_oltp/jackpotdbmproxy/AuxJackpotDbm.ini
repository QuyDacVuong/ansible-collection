; {{ ansible_managed }}
;@d:\apps\configs\c_oltp\sharedOLAPs.ini

;DATABASE CONNECTION HANDLED VIA ESL

[MAINSTORAGE]
;dsname={{ db_config['dbs']['zoneS-oltp']['dbname'] }}
;login=db2pyr
;
schema=db2admin

[STATICSETTINGS]

[TABLES] 
DBMID=1522


[;SETTINGS]
olap=OS127
olap=OS128
olap=OS124

[TOB]
schema=outbox
; same prefix as tobrelay.ini
prefix=jackpot

[LOGGER]
server=ip:{{ hercules_host }}:9322
LogStationName={{ stationname }}.C_OLTP.AUXJACKPOTDBM-{{ hostvars[inventory_hostname]['private_ip_address'].split('.')[-1] }}
UseLogger=1
