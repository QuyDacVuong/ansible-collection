;CONNECTION HANDLED VIA ESL - STATIC + DYNAMIC

[STARTUP]
{% if lobby_auxlobbyetc_startup %}
{{ lobby_auxlobbyetc_startup }}
{% endif %}

[STATICCONNECT]
{% if lobby_auxlobbyetc_staticconnect %}
{{ lobby_auxlobbyetc_staticconnect }}
{% endif %}

[DYNAMICCONNECT]
{% if lobby_auxlobbyetc_dynamicconnect %}
{{ lobby_auxlobbyetc_dynamicconnect }}
{% endif %}

[DYNAMICSETTINGS]
{% if lobby_auxlobbyetc_dynamicsettings %}
{{ lobby_auxlobbyetc_dynamicsettings }}
{% endif %}

[STATICSETTINGS]
{% if lobby_auxlobbyetc_staticsettings %}
{{ lobby_auxlobbyetc_staticsettings }}
{% endif %}

[LOGGER]
loggerAddress=ip:{{ hercules_host }}:9322
LogStationName={{ stationname }}.C_LOBBY.AuxLobbyEtc-{{ hostvars[inventory_hostname]['private_ip_address'].split('.')[-1] }}
useLogger=1

[telemetry.targets]
hercules = true
gateway = true
[telemetry.gateway.conn-params]
address = ip:127.0.0.1:5447
instance = TelgatewayInstance
[telemetry.metrics.tags]
application = AuxLobbyEtc
environment = {{ hostvars[inventory_hostname].tags.Environment }}
name = {{ hostvars[inventory_hostname].tags.Name }}
instanceid = {{ hostvars[inventory_hostname].instance_id }}
platform = {{ hostvars[inventory_hostname].tags.Platform }}
zone = {{ hostvars[inventory_hostname].tags.Zone }}
station_name = {{ stationname }}.C_LOBBY.AuxLobbyEtc-{{ hostvars[inventory_hostname]['private_ip_address'].split('.')[-1] }}
service_name = AuxLobbyEtc
[telemetry.metrics.filters.1]
regex = .*
